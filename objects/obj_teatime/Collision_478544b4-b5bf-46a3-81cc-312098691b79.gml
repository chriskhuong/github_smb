/// @description Insert description here
// You can write your code in this editor


//set up a check for game mode, in case of PVP

with (other)
	{
		if (tag == "enemy")
			{
				max_speed = max_speed / 2;
			}
	}